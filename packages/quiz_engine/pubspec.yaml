name: quiz_engine
description: Reusable quiz models and pure logic for KOMADEKI quiz apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  crypto: ^3.0.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.8
