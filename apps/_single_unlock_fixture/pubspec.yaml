name: single_unlock_fixture
description: Temporary independent shell proving quiz_engine app boundaries.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  quiz_engine:
    path: ../../packages/quiz_engine

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/question_bank/qualification_fixture_bank.json
